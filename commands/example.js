module.exports = {
    name: "title of command",
    description: "desc. of command",
    execute(msg, args) {
        // npm libraries
        const Discord = require("discord.js");

        // functions that need to be defined beforehand
        var botversion = "ver. 1.0.1";

        // code starts here
        null
    }
}